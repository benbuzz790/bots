Work Order for python_edit.py Implementation
Files:
- bots/tools/python_edit.py: Main implementation
- tests/test_python_edit.py: Test suite
Current Status:
- Fixed infinite loop in tokenize_source that was causing test timeouts
- Issue was tokens being created with quotes, causing regex to match them again
- Fixed by removing quotes from token format in create_token()
- Fixed tokenization of inline comments
- Removed condition that prevented tokenizing comments on lines with tokens
- Preserved exact spacing between code and inline comments
- Fixed line insertion functionality
- Updated _handle_insertion to detokenize lines before comparison
- Correctly insert new AST nodes at the right position in the function body
- Fixed scope-based insertion (test_insert_after_scope)
- Added logic to handle inserting after a child scope when at parent level
- Fixed nested indentation for line insertion (test_line_insert_indentation)
- Added _find_containing_node and _insert_into_node helper methods
- Now correctly finds the deepest AST node with a body that contains the target line
- Fixed comment preservation through AST reconstruction
- Changed token format to ;"""hexcontent""" to make tokens valid Python code
- Updated detokenization to handle AST unparsing behavior
- 39 tests passing (up from 38), 5 tests failing (down from 6)
Completed:
- Basic tokenization system working correctly
- Token system uses ;"""hexcontent""" format for valid Python syntax
- Successfully handles:
- Basic expressions
- Comments (inline and standalone)
- String literals (single and multi-line)
- Nested structures
- Complex indentation
- Line insertion after specific lines now works
- Can insert code after comments
- Preserves indentation at function level
- Handles tokenized content correctly
- NOW WORKS: Correctly maintains indentation in nested blocks (if/for/while)
- Scope-based insertion now works
- Can insert after 'OuterClass::another_method' when editing 'OuterClass'
- Properly finds child nodes and inserts after them
- Comment preservation now partially works
- Comments are preserved through AST reconstruction
- test_line_insert_ast_handling now passes
Remaining Issues:
1. Some source preservation tests still failing (5 tests)
   - test_line_insert_preservation: Issue with 'pass' statements
   - test_line_insert_function_body: Timeout issue
   - test_source_preservation: Complex formatting preservation
   - test_ast_tree_preservation: File-level comment ordering
   - test_file_level_preservation: Comment ordering with __FILE_START__
2. Performance issue causing timeout
   - test_line_insert_function_body times out after 60 seconds
   - Likely an infinite loop in detokenization
Next Steps:
1. Debug the timeout issue in test_line_insert_function_body
   - Add debugging to identify infinite loop in detokenization
   - May be related to complex token replacement patterns
2. Fix remaining source preservation issues
   - Some tests expect exact preservation of blank lines and formatting
   - May need to refine the detokenization logic
3. Consider performance optimizations
   - Current detokenization uses nested loops that could be optimized
Priority: Fix the timeout issue first, then address the remaining preservation tests. The tool is now functional for most use cases including nested indentation and basic comment preservation."